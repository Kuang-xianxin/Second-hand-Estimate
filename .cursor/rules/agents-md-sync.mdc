---
description: AGENTS.md 项目记忆同步规则
alwaysApply: true
---

# AGENTS.md 项目记忆管理

## 核心原则

每个项目都应该有一份 `AGENTS.md`，作为 AI 理解该项目的唯一权威来源。

## 理解项目时的优先级

1. **先查找 `AGENTS.md`**：当被要求了解、阅读、分析一个项目时，**首先**检查项目根目录是否存在 `AGENTS.md`
2. **有文件 → 以它为准**：如果存在，读取并严格依据 `AGENTS.md` 中的内容来理解项目结构、技术栈、工作流程、注意事项等
3. **无文件 → 立即创建**：如果不存在，在充分了解项目后，**立刻**在项目根目录创建一份 `AGENTS.md`，包含：
   - 项目是什么、做什么的
   - 技术栈和架构概览
   - 目录结构说明
   - 关键工作流程
   - 开发注意事项
   - 部署/运行方式

## 修改项目时同步更新

对项目进行任何修改（包括新增功能、修改架构、重构代码、更新依赖、改变工作流程等）时：

1. **同步更新 `AGENTS.md`**：修改完成后，立即检查并更新 `AGENTS.md` 中对应的内容，保持文档与代码同步
2. **不要只改代码不改文档**：如果新增了 API、新目录、新模块，必须同步反映到 `AGENTS.md` 中
3. **更新而非重写**：只修改受影响的部分，保持文档其他内容不变

## 文件命名

- 文件名必须为 `AGENTS.md`（全大写），放在项目根目录
- 不使用 `AGENTS.md` 以外的名字（如 `agent.md`、`README_AGENTS.md` 等）
