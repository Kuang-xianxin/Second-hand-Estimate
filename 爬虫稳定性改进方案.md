# 爬虫稳定性改进方案

## 架构理解


| 层    | 频率     | 范围         | 目的           |
| ---- | ------ | ---------- | ------------ |
| 市场价格 | 每天 1 次 | 1991 × 9 页 | 建立全量价格基准     |
| 捡漏广场 | 每 1.5h | 1991 × 9 页 | 找出当前远低于均价的商品 |


**核心目标：让全量爬取本身更可靠、更稳定，而不是削减爬取范围。**

---

## 问题根因分析

### 问题 1：Page.goto 超时（占比最大）

```
TimeoutError: Page.goto Timeout 30000ms exceeded
```

- 闲鱼页面静态资源多，30s 等待 `load` 事件不够
- 并发 8 个 Tab 同时请求，加剧服务器压力
- 一次超时 = 白等 30s 后失败

### 问题 2：asyncio CancelledError 引发 EPIPE 崩溃

```
asyncio.exceptions.CancelledError
→ Playwright pipe 通信断裂
→ Node.js 进程抛出 EPIPE: broken pipe
→ 浏览器驱动崩溃
→ 后端进程被迫终止
```

当超时触发 asyncio 取消操作时，`handle_response` 中的 `response.body()` 调用被中断，但代码未捕获 `CancelledError`，导致未处理异常向上传播，触发 Playwright driver 的 pipe write 断裂。

### 问题 3：异常扩散导致上下文泄漏

超时后，旧的 browser context/page 没有被正确关闭，积累到一定数量后资源耗尽。

---

## 改进方案

---

### 1. 放宽页面加载策略（高优先级）

**文件**: `backend/app/crawler/xianyu.py`

```python
# 改前
page.goto(url, wait_until="load", timeout=30000)

# 改后
page.goto(url, wait_until="domcontentloaded", timeout=60000)
```

**原因**：

- `domcontentloaded` 只等待 HTML 解析完成，不等待图片/CSS/字体等子资源加载
- 闲鱼商品列表的 HTML 结构在 domcontentloaded 时已经就绪
- 超时从 30s 提升到 60s，给网络波动更多缓冲
- 预估成功率提升：~50% → ~75%

---

### 2. 降低并发数（高优先级）

**文件**: `backend/app/services/crawl_worker.py`

```python
# 改前
MAX_CONCURRENCY = 8

# 改后
MAX_CONCURRENCY = 3
```

**原因**：

- 8 并发对闲鱼服务器压力大，容易触发反爬
- 3 并发足够保证吞吐量，同时大幅降低被限流概率
- 配合超时延长，总耗时不会显著增加

---

### 3. 修复 CancelledError + EPIPE 崩溃（高优先级）

**文件**: `backend/app/crawler/xianyu.py`

在 `handle_response` 中捕获 CancelledError：

```python
async def handle_response(self, response):
    try:
        body = _try_decode_body(response.body())
    except asyncio.CancelledError:
        logger.warning("Response handler cancelled, skip body decode")
        raise  # 重新抛出，让调用方处理 context 清理
    except Exception as e:
        logger.debug(f"Body decode error (ignored): {e}")
        return
```

在 `_scrape_sync` 的超时处理中，确保 context 正确关闭：

```python
try:
    page.goto(...)
except TimeoutError:
    logger.warning(f"关键词'{keyword}'爬取超时")
    # 不再在这里触发 context 泄漏
finally:
    # 由外部统一管理 context 生命周期
    pass
```

**加上 driver 的 EPIPE 错误处理**（防止未捕获的 driver 进程错误导致崩溃）：

```python
# 在启动 browser 时
browser = pw.chromium.launch(
    args=[
        "--no-sandbox",
        "--disable-dev-shm-usage",
        "--disable-gpu",
        "--disable-software-rasterizer",
    ]
)
```

---

### 4. 指数退避重试（中优先级）

**文件**: `backend/app/services/crawl_worker.py`

```python
async def crawl_keyword_with_retry(keyword, max_retries=2):
    for attempt in range(max_retries + 1):
        try:
            return await crawl_single_keyword(keyword)
        except TimeoutError as e:
            if attempt < max_retries:
                wait_time = 3 * (attempt + 1)  # 3s, 6s
                logger.info(f"关键词'{keyword}'超时，第{attempt+1}次重试，等待{wait_time}s")
                await asyncio.sleep(wait_time)
            else:
                logger.warning(f"关键词'{keyword}'重试耗尽，跳过")
                return None
```

- 对偶发超时（网络抖动）自动恢复
- 不对系统性封禁做无意义重试（第 2 次超时后直接跳过）

---

### 5. 批次间延迟（中优先级）

**文件**: `backend/app/services/crawl_worker.py`

```python
async def run_batch(self, batch_keywords, batch_index, total_batches):
    # 批次间增加随机延迟 2~5s，降低闲鱼服务端限流风险
    if batch_index > 0:
        delay = random.uniform(2, 5)
        logger.info(f"批次 {batch_index}/{total_batches}，等待 {delay:.1f}s")
        await asyncio.sleep(delay)
```

---

### 6. Playwright driver 稳定性（低优先级）

**原因**：当前 `playwright._impl._sync_base.py` 底层的 EPIPE 是 Playwright 自身的问题，但以下配置可以缓解：

```python
# browser 启动参数
args=[
    "--disable-blink-features=AutomationControlled",
    "--disable-dev-shm-usage",
    "--no-sandbox",
    "--disable-setuid-sandbox",
    "--disable-web-security",
]
```

- `--disable-dev-shm-usage`：防止 Docker/Linux 下的共享内存不足（Windows 下也推荐）
- `--disable-web-security`：避免 CORS 等干扰

---

### 7. DrissionPage 替代 Playwright（后续考虑）

当前代码同时引入了 Playwright 和 DrissionPage，但爬虫核心用的是 Playwright。

DrissionPage 对闲鱼的适配更好，且原生支持异步模式，与 asyncio 事件循环一致性好，**可以避免 CancelledError 问题**。

但迁移有一定风险，建议作为后续优化项。

---

## 改动清单


| 优先级 | 措施                          | 文件                | 改动量        |
| --- | --------------------------- | ----------------- | ---------- |
| P0  | `domcontentloaded` + 超时 60s | `xianyu.py`       | 2 行        |
| P0  | 并发 8 → 3                    | `crawl_worker.py` | 1 行        |
| P0  | 修复 CancelledError 处理        | `xianyu.py`       | ~10 行      |
| P1  | 指数退避重试（最多 2 次）              | `crawl_worker.py` | ~20 行      |
| P1  | 批次间随机延迟 2~5s                | `crawl_worker.py` | ~5 行       |
| P1  | browser launch 稳定参数         | `xianyu.py`       | ~3 行       |
| P2  | DrissionPage 迁移             | `xianyu.py`       | ~200 行（后续） |


---

## 预估效果


| 指标          | 改前           | 改后         |
| ----------- | ------------ | ---------- |
| 单次爬取成功率     | ~50%         | ~75%+      |
| 超时等待时间      | 30s/次        | 60s/次      |
| 进程崩溃概率      | 高（EPIPE）     | 极低         |
| 1991 关键词总耗时 | 45min+（崩溃终止） | ~60min（完成） |


---

请确认方案，我立即开始实施 P0 优先级的改动。